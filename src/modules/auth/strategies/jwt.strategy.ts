// JWT Strategy placeholder
// TODO: Implement Google OAuth strategy in Phase 1 when credentials are available

export const jwtStrategy = {
  name: 'jwt',
  // Strategy is handled via auth.middleware.ts using jsonwebtoken directly
};
