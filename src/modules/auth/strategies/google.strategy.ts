// Google OAuth Strategy placeholder
// TODO: Implement with google-auth-library when credentials are provided

export const googleStrategy = {
  name: 'google',
  // Will verify Google ID tokens and extract user info
};
